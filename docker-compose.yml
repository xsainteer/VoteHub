version: '3.8'

services:
  postgres:
    image: postgres
    container_name: votehub
    restart: always
    environment:
      POSTGRES_USER: xsainteer
      POSTGRES_PASSWORD: password
      POSTGRES_DB: votehub
    ports:
      - "5432:5432"